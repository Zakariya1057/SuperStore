//
//  AdvancedSettingsRouter.swift
//  SuperStore
//
//  Created by Zakariya Mohummed on 07/06/2021.
//  Copyright (c) 2021 Zakariya Mohummed. All rights reserved.
//
//  This file was generated by the Clean Swift Xcode Templates so
//  you can apply clean architecture to your iOS and Mac projects,
//  see http://clean-swift.com
//

import UIKit

@objc protocol AdvancedSettingsRoutingLogic
{
    func routeToEditName(segue: UIStoryboardSegue?)
    func routeToEditEmail(segue: UIStoryboardSegue?)
    func routeToEditPassword(segue: UIStoryboardSegue?)
    
    func routeToEditRegion(segue: UIStoryboardSegue?)
    func routeToEditStore(segue: UIStoryboardSegue?)
    
    func routeToDeviceStorage(segue: UIStoryboardSegue?)
    
    func routeToSettings(segue: UIStoryboardSegue?)
    
    func routeToFeedback(segue: UIStoryboardSegue?)
}

protocol AdvancedSettingsDataPassing
{
    var dataStore: AdvancedSettingsDataStore? { get }
}

class AdvancedSettingsRouter: NSObject, AdvancedSettingsRoutingLogic, AdvancedSettingsDataPassing
{
    weak var viewController: AdvancedSettingsViewController?
    var dataStore: AdvancedSettingsDataStore?
    
    // MARK: Routing
    
    
    func routeToEditRegion(segue: UIStoryboardSegue?)
    {
        if let segue = segue {
            let destinationVC = segue.destination as! EditRegionViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditRegion(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "EditRegionViewController") as! EditRegionViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditRegion(source: dataStore!, destination: &destinationDS)
            navigateToEditRegion(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToEditStore(segue: UIStoryboardSegue?)
    {
        if let segue = segue {
            let destinationVC = segue.destination as! EditStoreViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditStore(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "EditStoreViewController") as! EditStoreViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditStore(source: dataStore!, destination: &destinationDS)
            navigateToEditStore(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToEditName(segue: UIStoryboardSegue?)
    {
        if let segue = segue {
            let destinationVC = segue.destination as! EditNameViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditName(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "EditNameViewController") as! EditNameViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditName(source: dataStore!, destination: &destinationDS)
            navigateToEditName(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToEditEmail(segue: UIStoryboardSegue?)
    {
        if let segue = segue {
            let destinationVC = segue.destination as! EditEmailViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditEmail(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "EditEmailViewController") as! EditEmailViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditEmail(source: dataStore!, destination: &destinationDS)
            navigateToEditEmail(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToEditPassword(segue: UIStoryboardSegue?)
    {
        if let segue = segue {
            let destinationVC = segue.destination as! EditPasswordViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditPassword(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "EditPasswordViewController") as! EditPasswordViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToEditPassword(source: dataStore!, destination: &destinationDS)
            navigateToEditPassword(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToFeedback(segue: UIStoryboardSegue?)
    {
        if let segue = segue {
            let destinationVC = segue.destination as! FeedbackViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToFeedback(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "FeedbackViewController") as! FeedbackViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToFeedback(source: dataStore!, destination: &destinationDS)
            navigateToFeedback(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToSettings(segue: UIStoryboardSegue?){
        if let segue = segue {
            let destinationVC = segue.destination as! SettingsViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToSettings(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "SettingsViewController") as! SettingsViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToSettings(source: dataStore!, destination: &destinationDS)
            navigateToSettings(source: viewController!, destination: destinationVC)
        }
    }
    
    func routeToDeviceStorage(segue: UIStoryboardSegue?){
        if let segue = segue {
            let destinationVC = segue.destination as! DeviceStorageViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToDeviceStorage(source: dataStore!, destination: &destinationDS)
        } else {
            let storyboard = UIStoryboard(name: "Main", bundle: nil)
            let destinationVC = storyboard.instantiateViewController(withIdentifier: "DeviceStorageViewController") as! DeviceStorageViewController
            var destinationDS = destinationVC.router!.dataStore!
            passDataToDeviceStorage(source: dataStore!, destination: &destinationDS)
            navigateToDeviceStorage(source: viewController!, destination: destinationVC)
        }
    }
    
    // MARK: Navigation
    
    func navigateToEditRegion(source: AdvancedSettingsViewController, destination: EditRegionViewController)
    {
        source.show(destination, sender: nil)
    }
    
    func navigateToEditStore(source: AdvancedSettingsViewController, destination: EditStoreViewController)
    {
        source.show(destination, sender: nil)
    }
    
    func navigateToEditName(source: AdvancedSettingsViewController, destination: EditNameViewController)
    {
        source.show(destination, sender: nil)
    }
    
    func navigateToEditEmail(source: AdvancedSettingsViewController, destination: EditEmailViewController)
    {
        source.show(destination, sender: nil)
    }
    
    func navigateToEditPassword(source: AdvancedSettingsViewController, destination: EditPasswordViewController)
    {
        source.show(destination, sender: nil)
    }
    
    func navigateToFeedback(source: AdvancedSettingsViewController, destination: FeedbackViewController)
    {
        source.show(destination, sender: nil)
    }

    func navigateToSettings(source: AdvancedSettingsViewController, destination: SettingsViewController)
    {
        source.navigationController?.popViewController(animated: true)
    }
    
    func navigateToDeviceStorage(source: AdvancedSettingsViewController, destination: DeviceStorageViewController)
    {
        source.show(destination, sender: nil)
    }

    // MARK: Passing data
    
    func passDataToEditRegion(source: AdvancedSettingsDataStore, destination: inout EditRegionDataStore)
    {
        
    }
    
    func passDataToEditStore(source: AdvancedSettingsDataStore, destination: inout EditStoreDataStore)
    {
        
    }

    
    func passDataToEditName(source: AdvancedSettingsDataStore, destination: inout EditNameDataStore)
    {
        destination.name = source.user!.name
    }
    
    func passDataToEditEmail(source: AdvancedSettingsDataStore, destination: inout EditEmailDataStore)
    {
        destination.email = source.user!.email
    }
    
    func passDataToEditPassword(source: AdvancedSettingsDataStore, destination: inout EditPasswordDataStore)
    {
        
    }
    
    func passDataToFeedback(source: AdvancedSettingsDataStore, destination: inout FeedbackDataStore)
    {
        destination.setting = source.selectedSetting
    }
    
    func passDataToSettings(source: AdvancedSettingsDataStore, destination: inout SettingsDataStore)
    {
    }
    
    func passDataToDeviceStorage(source: AdvancedSettingsDataStore, destination: inout DeviceStorageDataStore)
    {
    }
}
